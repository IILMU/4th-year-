{
  "name": "leafsense",
  "private": true,
  "description": "LeafSense — Flask backend + React frontend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "start": "npm run dev --prefix frontend"
  }
}
